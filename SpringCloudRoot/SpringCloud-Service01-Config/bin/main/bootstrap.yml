server:
  port: 18080

spring:
  application:
    name: config
  profiles:
    active: native
  cloud:
    config:
      server:
        prefix: /prefix-config
        native:
          searchLocations: classpath:/configs
